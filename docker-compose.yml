

services:
  backend:
    build: ./backend             # đường dẫn tới thư mục chứa Dockerfile backend
    container_name: backend
    ports:
      - "5000:5000"              # ánh xạ cổng backend ra ngoài
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=5000

  frontend:
    build: ./frontend               # <--- thêm dòng này để build từ mã nguồn
    image: webbanphukienmaytinh-frontend:latest
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
